# Elephpant project core

To get this personalized mini framework, create a new folder, navigate to that folder and run the following commands:

```
composer init --require angelokezimana/elephpant-project-core:*
```

```
composer update
```

And start hacking ! 💻
